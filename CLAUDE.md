# YUKISIKI小顔矯正 LP

## プロジェクト概要
- **サイト**: https://yukisiki.com
- **事業**: 小顔・顔の歪み矯正専門サロン（中目黒）
- **技術**: Next.js 16 (App Router) + TypeScript + Tailwind CSS
- **デプロイ**: Vercel

## サイト構成
| パス | ページ | 目的 |
|------|--------|------|
| `/` | トップLP | メインランディングページ。ファーストビュー→悩み→施術内容→実績→料金→アクセス→CTA |
| `/kogao` | 小顔矯正とは | 小顔矯正の詳細解説ページ |
| `/yugami` | 顔の歪み矯正とは | 顔の歪み矯正の詳細解説ページ |
| `/case` | 症例解説 | ビフォーアフター・症例紹介 |

## 計測設定
| 項目 | 値 |
|------|-----|
| GA4 Measurement ID | `G-40VLSMNGMS` |
| Clarity Project ID | `w4c1pys67a` |
| 環境変数 (Vercel) | `NEXT_PUBLIC_GA_ID` / `NEXT_PUBLIC_CLARITY_ID` |

### 計測コンポーネント
- `src/components/GoogleAnalytics.tsx` — GA4 gtag.js（afterInteractive）
- `src/components/MicrosoftClarity.tsx` — Clarity タグ（afterInteractive）

## 主要キーワード（SEOターゲット）
- 小顔矯正 中目黒
- 顔の歪み矯正 東京
- 小顔矯正 おすすめ
- フェイスライン 矯正
- 顔の左右非対称 矯正
- 小顔矯正 効果
- エラ張り 矯正

## 競合
- 小顔矯正サロン（中目黒・恵比寿・代官山エリア）
- 美容整体・小顔エステ系サロン

---

## 自動実行モード

ユーザーが以下のキーワードだけを入力した場合、確認なしで全ステップを順番に自動実行する。

### 「分析」と入力された場合

以下を順番にすべて実行する（コードの変更は一切しない）：

1. **サイト構造分析**
   - 全ページ一覧と各ページのターゲット・目的を整理
   - ページ間の導線を確認（内部リンク構造、CTA配置）
   - サイトマップ・robots.txtの状態を確認

2. **SEOスコアカード**
   - 各ページのmeta title/description一覧と文字数チェック
   - 構造化データ（JSON-LD）の有無と内容
   - 画像alt属性の充実度
   - Core Web Vitals に影響する要素（画像サイズ、フォント読み込み、JS量）
   - canonical URL・OGP設定の確認

3. **GA4データ分析**（Clarity MCPツールでも確認）
   - セッション数・ユーザー数・PV数の推移
   - ページ別のパフォーマンス（PV、滞在時間、直帰率）
   - 流入元の内訳（オーガニック、直接、リファラル、SNS）
   - デバイス別の利用比率

4. **Clarity分析**（Microsoft Clarity MCPツールを使用）
   - セッション数・ユニークユーザー数
   - デッドクリック率・レイジクリック率
   - スクロールの奥行き
   - ユーザーの意図分布（Low/Medium/High Intention）
   - ページ別のアクティブ時間
   - 特に問題のあるセッション録画のサンプリング

5. **コンテンツギャップ分析**
   - 主要キーワードに対する既存コンテンツのカバー率
   - 「小顔矯正」「顔の歪み矯正」関連で書くべきなのに書いていないテーマ
   - 競合が扱っていて自分が扱っていないテーマを推定
   - FAQ系コンテンツの充実度

6. **CTA・コンバージョン分析**
   - CTAボタンの配置・文言・デザインの一覧
   - CTA到達率の推定（スクロール深度 × CTA位置）
   - 予約導線の確認（LINE / ホットペッパー等への誘導）

7. **改善優先度マトリクス**
   - 影響度（大/中/小）× 工数（大/中/小）で分類
   - 最優先の改善3つを具体的なアクションとして提示

8. **レポート出力**
   - 上記をmarkdown形式で整理して表示
   - ファイルには保存しない（表示のみ）

### 「集客」と入力された場合

以下を順番にすべて実行する：

1. **SEO現状チェック**
   - `src/app` 配下の全ページのmeta title/descriptionを一覧化
   - 改善が必要なmeta情報があればその場で修正
   - 構造化データ（LocalBusiness等）の追加・修正

2. **GA4/Clarity分析**（データアクセス可能な場合）
   - 直帰率が高いページを特定
   - Clarityのヒートマップで離脱ポイントを推定

3. **LP改善の実行**
   - CTAボタンの文言・配置を確認し、改善案があれば実装
   - ファーストビューの訴求力をチェック、弱ければコピー修正
   - 内部リンク構造を確認し、回遊性を改善

4. **SEOコンテンツの改善**
   - 各ページのh2/h3の階層構造を確認・修正
   - メタディスクリプションを最適化
   - alt属性の補完

5. **結果レポート**
   - 変更した内容を箇条書きでまとめる
   - 次にやるべきアクション3つを提示
   - `npm run build` でエラーがないことを確認

### 「制作」と入力された場合

以下を順番にすべて実行する：

1. **リール台本を1本作成**
   - YUKISIKIブランドの新作（60秒、350-480文字）
   - 130万再生パターンの法則に従う（敵キャラ主人公、CTAなし、叫んで終わる）
   - キャラ例: たるみくん / むくみくん / 歪みくん
   - シーン分割（10秒×4-7本）で出力

2. **Gemini画像生成プロンプトを出力**
   - 各シーンのキャラ画像生成用プロンプトをコピペ可能な形で出力

3. **Grok動画生成プロンプトを出力**
   - 各シーンごとにGrok用プロンプトを出力
   - キャラの外見描写を絶対に入れない（最重要ルール）
   - `No speech bubbles, no text overlays` を必ず含める
   - `Duration: 10 seconds.` を末尾に必ず入れる

4. **LPコンテンツの追加**
   - SEO優先度の高いテーマを選定（既存ページと被らないもの）
   - 新規ページまたは既存ページへのセクション追加
   - 2000-3000文字、h2/h3構造、内部リンク含む

---

## Clarity MCP 分析テンプレート

分析モード実行時、Microsoft Clarity MCPツールを以下の順序で使用する：

### ステップ1: ダッシュボード概要
```
query: "セッション数、ユニークユーザー数、デッドクリック率、スクロールの奥行き 過去7日間"
```

### ステップ2: デバイス別分析
```
query: "デバイス別のセッション数と平均セッション時間 過去7日間"
```

### ステップ3: ページ別パフォーマンス
```
query: "ページ別のセッション数とアクティブ時間 過去7日間"
```

### ステップ4: ユーザー行動の問題点
```
query: "レイジクリックとデッドクリックが多いページ 過去7日間"
```

### ステップ5: 問題セッションの録画確認
```
filters:
  - date: 過去7日間
  - rageClickPresent: true または deadClickPresent: true
  - count: 10
```

---

## 発動ワード一覧

### 分析モード
`分析` `レポート` `数字見て` `状況どう？` `今どうなってる？` `改善点` `課題` `ボトルネック` `KPI` `パフォーマンス` `アクセス解析` `データ見て` `振り返り` `チェックして` `どこが悪い？` `Clarity` `GA4`

### 集客モード
`集客` `SEO` `流入増やして` `アクセス増やして` `検索順位` `LP改善` `導線改善` `CVR改善` `もっと見てもらいたい` `お客さん増やして`

### 制作モード
`制作` `作って` `書いて` `リール` `台本` `コンテンツ` `投稿` `ネタ` `新しいの作って` `ページ作って`

---

## プラグインスキル自動活用

| モード | 使用スキル | タイミング |
|--------|-----------|-----------|
| 分析 | Clarity MCPツール (`query-analytics-dashboard`, `list-session-recordings`) | GA4/Clarityデータ取得時 |
| 分析 | `/analyze` | データの深掘り分析 |
| 分析 | `/build-dashboard` | 分析結果をHTMLダッシュボードで可視化 |
| 分析 | `/create-viz` | グラフ・チャート生成 |
| 集客 | `/competitive-brief` | 競合分析 |
| 集客 | `/seo-audit` | SEO監査 |
| 制作 | `/draft-content` | コンテンツ作成 |
| 制作 | `/brand-review` | トーンチェック |
